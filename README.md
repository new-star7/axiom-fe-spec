# Axiom 前端规范工程化 (axiom-fe-spec)

本项目包含了 Axiom 团队前端工程化的各项规范配置，旨在统一团队代码风格，提升代码质量和协同效率。

## 包含的规范包

* `commitlint-config`: Git 提交规范校验
* `markdownlint-config`: Markdown 文档编写规范校验

## 如何使用本地文档

本项目配套了完整的在线说明文档。在本地开发时，可以通过以下命令启动文档网站：

```bash
pnpm install
pnpm run docs:dev
